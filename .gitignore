# Dependencies
# The node_modules directory contains all of your project's dependencies.
# It can be very large and should be generated by running `npm install`.
/node_modules

# Environment Variables
# The .env file contains secrets like API keys and database credentials.
# It should NEVER be committed to version control for security reasons.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
# Log files are generated during runtime and are specific to your machine.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build output
# This directory often contains compiled or bundled code for production.
/dist
/build

# Operating System files
# These are metadata files created by macOS, Windows, and Linux.
.DS_Store
Thumbs.db
ehthumbs.db

# IDE and Editor configuration
# User-specific settings for editors like VS Code.
.vscode/
.idea/
